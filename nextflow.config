//2. Write a Nextflow config file and add the publishDir directive in the process scope.
//Specify an output path and set mode to copy. 

process {
    publishDir = [
        path: './results',
        mode: 'copy'
    ]
}